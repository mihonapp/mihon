# Reading-List Progress

Normal chapter read state remains shared with the Mihon-derived library and history model.

Reading-list-specific state records the user's current position, skipped entries, and completion for that particular list. The same chapter may appear in multiple reading lists without duplicating its global read state.

An unresolved entry remains part of the list and may be skipped for a session without being deleted or marked resolved.
