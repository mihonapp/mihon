# Matching Evidence Versioning

Persist the matcher algorithm or evidence-version identifier with automatic results and rejected candidates.

When normalization or scoring changes materially, Yomori may offer to reevaluate automatic and unresolved entries. User-confirmed mappings remain protected. Rejected candidates may be reconsidered only when the evidence version or underlying remote metadata changed, and this behavior must be disclosed.
