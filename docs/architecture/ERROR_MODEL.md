# Import and Matching Error Model

Errors are categorized so the UI can distinguish malformed CBL data, unsupported format, source unavailable, authentication required, rate limited, remote parsing failure, no candidates, ambiguous candidates, missing chapter, and persistence failure.

A source or network error does not reduce a candidate's semantic confidence. It produces an operational state that can be retried.
