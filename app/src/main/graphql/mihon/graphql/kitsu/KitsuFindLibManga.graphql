query KitsuFindLibManga(
  $remote_id: ID!,
) {
  findMangaById(
    id: $remote_id,
  ) {
    ...MangaFragment
    myLibraryEntry {
      id
      private
      progress
      rating
      status
      startedAt
      finishedAt
    }
  }
}
