mutation KitsuDeleteLibEntry(
  $library_id: ID!
) {
  libraryEntry {
    delete(
      input: {
        id: $library_id
      }
    ) {
      errors {
        message
      }
      libraryEntry {
        id
      }
    }
  }
}
