mutation SuwayomiMarkAndDeleteChapters(
  $chapters: [Int!]!
) {
  updateChapters(
    input: {
      ids: $chapters,
      patch: {
        isRead: true
      }
    }
  ) {
    __typename
  }

  deleteDownloadedChapters(
    input: {
      ids: $chapters
    }
  ) {
    __typename
  }
}
