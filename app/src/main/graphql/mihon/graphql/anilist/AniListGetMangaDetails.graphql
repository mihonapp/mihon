query AniListGetMangaDetails($manga_id: Int!) {
  Page (perPage: 1) {
    media(id: $manga_id, type: MANGA, format_not_in: [NOVEL]) {
      id
      staff {
        edges {
          role
          id
          node {
            name {
              full
              userPreferred
              native
            }
          }
        }
      }
      title {
        userPreferred
      }
      coverImage {
        large
      }
      format
      countryOfOrigin
      status
      chapters
      description
      startDate {
        year
        month
        day
      }
      averageScore
    }
  }
}
